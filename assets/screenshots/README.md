# 截图目录说明

请将应用界面截图放在此目录，并使用以下固定文件名，便于 `README.md` 自动展示：

- `main-window.png`：主界面
- `settings-deps.png`：设置中的依赖中心
- `task-preview.png`：开始转换前的任务预览弹窗

建议尺寸：宽度 1280 左右，PNG 格式，避免过度压缩。

版本发布建议使用子目录保留历史截图，例如：`v1.2/main-window.png`。
这样可以避免同一路径缓存不刷新，也能防止新版本覆盖旧版本展示素材。

